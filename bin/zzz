#!/bin/bash

case $1 in
  hard)
    method="Hibernate"
  ;;
  *)
    method="Suspend"
  ;;
esac

dbus-send --print-reply --system --dest=org.freedesktop.UPower /org/freedesktop/UPower "org.freedesktop.UPower.$method"
