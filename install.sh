#!/bin/bash

# check for packages.

# check for ~/.0desk, link to the 0desk directory if not present.

# check files like .profile, .zlogin, .zshrc, etc for strings like PATH=,
# append '$HOME/.0desk/bin' if not present (using sed probably)

# link various config file to their correct locations in .config, ~, etc.

